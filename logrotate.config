# Configuration file for log rotation script
LOG_DIR="$HOME/devops-labs/logs"
RETENTION=5
MIN_SIZE=1048576     # 1 MB minimum size before rotation
CLEANUP_DAYS=30      # Delete archives older than 30 days

